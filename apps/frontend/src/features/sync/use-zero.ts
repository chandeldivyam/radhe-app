import {createUseZero} from '@rocicorp/zero/react';
import type {Schema} from '@radhe/zero-shared';
import type {Mutators} from '@radhe/zero-shared';
export const useZero = createUseZero<Schema, Mutators>();